type: application
language: python

project:
  name: fastapi
  description: 'Mocks the app'

tasks:
  run:
    command: 'uvicorn main:app --host 0.0.0.0 --port 8000'

  docker:
    command: 'docker build --network=host -t fastapi-app .'
